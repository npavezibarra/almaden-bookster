# Directorio settings (legacy)

Este directorio forma parte del plugin Almaden Bookster y se mantiene solo por compatibilidad.
La ubicación canonical para `Book Templates` es `includes/templates/book-templates/`.

Archivos y subdirectorios contenidos aquí:

* default-classic.json
